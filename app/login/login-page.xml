<?xml-stylesheet type="text/css" href="login-page.css"?>
<Page
    navigatingTo="onNavigatingTo"  
    xmlns="http://schemas.nativescript.org/tns.xsd">


    <ActionBar>
        <!-- 
        Use the NavigationButton as a side-drawer button in Android
        because ActionItems are shown on the right side of the ActionBar
        -->
        <NavigationButton ios:visibility="collapsed" icon="res://menu" tap="onDrawerButtonTap"></NavigationButton>
        <!-- 
        Use the ActionItem for IOS with position set to left. Using the
        NavigationButton as a side-drawer button in iOS is not possible,
        because its function is to always navigate back in the application.
        -->
        <ActionItem icon="res://navigation/menu" 
            android:visibility="collapsed" 
            tap="onDrawerButtonTap"
            ios.position="left">
        </ActionItem>
        <Label class="navTitle" text="You In?"></Label>
    </ActionBar>




    <FlexboxLayout class="page">
    <StackLayout class="form">
        <Label class ="title" text="Log In"/>
        <StackLayout class="input-field">
            <Label class="inputLabel" text="Username or Email"></Label>
            <TextField class="input"></TextField>
        </StackLayout>

        <StackLayout class="input-field">
            <Label class="inputLabel" text="Password"></Label>
            <TextField class="input"></TextField>
        </StackLayout>

        <Button text="Log In" class="button"></Button>

        <Label text="Don’t have an account?" class="{{ 'nt-drawer__list-item' + (selectedPage === 'Register' ? ' -selected': '') }} endLabel"
               route="register/register-page" tap="onNavigationItemTap"></Label>

    </StackLayout>
    </FlexboxLayout>


</Page>
